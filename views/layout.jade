- var profile_name = function() { 
-   var d = session_data == undefined ? 'Welcome, Guest' : session_data.name
-   return d;
- }
-
- var is_logged_in = function() {
-   console.log(session_data == undefined);
-   return session_data != undefined;
- }

doctype html
html
  head
    title= title
  body
    nav(class='navbar navbar-inverse')
      div(class='container-fluid')
        div(class='navbar-header')
          a(class='navbar-brand', href='/') Hitch.Me

        div(class='collapse navbar-collapse')
          ul(class='nav navbar-nav navbar-right')
            if !is_logged_in()
              li
                a(href='/login/facebook') Log In
            else
              li(class='dropdown')
                a(class='dropdown-toggle', href='#', data-toggle='dropdown')= profile_name()
                ul(class='dropdown-menu')
                  li
                    a(href='#') Profile
    block content

    script(src = 'https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js')
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
    link(rel='stylesheet', href='/stylesheets/style.css')
